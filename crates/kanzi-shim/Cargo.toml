[package]
name = "kanzi-shim"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "C-ABI shared library wrapping kanzi-cpp's compressor for ctypes (benchmark use)."
build = "build.rs"

[lib]
name = "kanzi_shim"
crate-type = ["cdylib"]

[build-dependencies]
cc = "1"
glob = "0.3"
