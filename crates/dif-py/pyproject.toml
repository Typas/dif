[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "dif"
version = "0.1.0"
description = "Python bindings for the DIF codec"
requires-python = ">=3.12,<3.13"
classifiers = ["Programming Language :: Rust"]

[tool.maturin]
features = ["pyo3/extension-module"]
